# Log (append-only)

Regista aqui uma linha por evento relevante. Nunca apagar ou reescrever linhas antigas — só acrescentar no fim.

- 2026-07-03 — second brain criado (`LOADER.md`, `index.md`, `AGENTS.md`, `raw/`, `wiki/`, `skills/brain-toggle`).
- 2026-07-03 — criado `MASTER_PROMPT.md` (retrato reproduzível do sistema atual: marcadores BRAIN:START/END nos 3 ponteiros, `org-context.md` remoto + espelho como plugins Claude Code, convenção skills fonte-única + symlink, brain-toggle soft/hard). Lint em `AGENTS.md` passa a exigir atualizá-lo sempre que a estrutura mudar.
- 2026-07-07 — regras React frontend em `context/react-frontend.md` + `LOADER.md` §7 (cross-tool); removido plugin Cursor-only.
- 2026-07-07 — removida skill `react-frontend` (era contexto, não skill).
- 2026-07-07 — `AGENTS.md`: secção «Contexto vs skills» + regra cross-tool; lint §5.
- 2026-07-08 — skill org `commit` renomeada para `commit-push` (evita colisão com SCM Cursor); `org-context.md` e `MASTER_PROMPT.md` actualizados; hook `beforeSubmitPrompt` bloqueia template SCM nativo.
- 2026-07-08 — `org-context`: `context/skills.md` (eliminar skill = remover todo o rastro); fetch em `org-context.md`.
- 2026-07-08 — criada skill org `commit-push`, mas marketplace do Claude Code ficou desactualizado (preso no commit de instalação, 2026-07-03) e a skill não apareceu; corrigido com `claude plugin marketplace update org-context` + `claude plugin update code-standards@org-context`. Nova regra cross-tool `context/org-skill-sync.md` + `LOADER.md` §8 + `MASTER_PROMPT.md` §7 (renumerado §7→§10) para tornar este passo automático da próxima vez.
- 2026-07-17 — `~/brain` clonado de fresco numa nova máquina/ambiente; recriados os 3 ponteiros (`~/AGENTS.md`, `~/.claude/CLAUDE.md`, `~/.codex/AGENTS.md`) via `brain-toggle on --hard`, symlink `~/.claude/skills/brain-toggle`, e regra de auto-symlink em `~/.claude/CLAUDE.md` (MASTER_PROMPT.md §5). Marketplace `org-context` **não** instalado: `git.planning4life.intranet` inacessível neste sandbox (sem VPN) — pendente, correr manualmente quando na rede certa. Corrigida nota desactualizada em `MASTER_PROMPT.md` §10 (`~/brain` já é repo git).
- 2026-07-17 — resolvido o pendente acima: `git.planning4life.intranet` afinal acessível (rede estava só instável/DNS flaky). Causa raiz de "skills não chegam" era dupla: (1) `WebFetch` faz upgrade forçado `http://`→`https://` e falha neste host sem TLS válido — `curl`/Bash directo funciona; (2) marketplace `org-context` nunca tinha sido registado neste Claude Code (`claude plugin marketplace add` + `claude plugin install` × 3 nunca corridos). Instalado marketplace + 3 plugins (`code-standards`, `project-setup`, `security`); `claude plugin install` já escreveu `enabledPlugins`/`extraKnownMarketplaces` em `~/.claude/settings.json` (mecanismo declarativo nativo, sem hook necessário). Para nunca mais depender de copiar `settings.json` entre máquinas: nova regra de self-heal no arranque da sessão em `~/.claude/CLAUDE.md` § "Marketplace org-context" (paralelo ao check de symlinks §5) — verifica e corrige automaticamente se o marketplace/plugins faltarem. `MASTER_PROMPT.md` §6 actualizado.
- 2026-07-20 — Cursor: org-context **só via fetch HTTP** fresco por sessão; ban explícito a `~/.claude/plugins/cache/org-context/**`. Actualizados `org-context.md`, `LOADER.md` §2/§8, `context/org-skill-sync.md`, `MASTER_PROMPT.md` §6–§7; em `org-context` `context/skills.md` deixa de mandar symlink para o cache Claude.
- 2026-07-20 — Corte mecânico Cursor: stubs fetch em `~/.cursor/skills/{commit-push,review,context-migration,security-review,codebase-review}`; docs exigem desligar «Include third-party Plugins…» (senão o IDE continua a injectar o cache Claude).
- 2026-07-28 — Fetch org-context no Cursor: obrigatório `curl` na Shell local; **proibido** WebFetch para `*.intranet` (WebFetch devolve 503). Actualizados `org-context.md`, `LOADER.md` §2, `MASTER_PROMPT.md` §6, stubs em `~/.cursor/skills/*`, e template em `org-context/context/skills.md`.
- 2026-08-06 — Drift `pen` corrigido: apagada cópia real `~/brain/skills/pen/SKILL.md` (skill de projecto no brain violava `AGENTS.md` § Contexto vs skills) + symlink aninhado `pen/pen`; `~/.cursor/skills/pen` repointado directamente para `~/Projects/EvPlanner/.claude/skills/pen`; `MASTER_PROMPT.md` §5 corrigido (pen é skill de projecto). Restaurados stubs fetch em `~/.cursor/skills/{commit-push,review,context-migration,security-review}` (estavam symlinks para o cache Claude — proibido) e criados stubs em falta `create-ticket`/`review-tickets`; `codebase-review` mantido como symlink para o clone local (padrão opcional aceite em `skills.md`).
- 2026-08-06 — Harness P0–P3 no EvPlanner (análise vs OpenAI harness-engineering): CI GitHub Actions nos 2 subprojectos; ESLint arquitectural (fetch/axios só em `src/data/`, mensagens com remediação para agentes) + import-linter (core↛domínio, settings↛apps — detectou e refactorizou 1 violação real: `count_selfie_cycles_until_target` movido para `snapshots.cycles`); ruff verde (106 erros corrigidos); mypy 103 erros herdados → CI não-bloqueante + tracker; pytest: colecção partida (pytest_plugins em conftest não-raiz — fixtures movidas para `categories/tests/fixtures.py` com registo no conftest raiz) e 36 testes vermelhos pré-existentes (drift datas/ciclos, `received_on` NOT NULL, knowledge) → quarentena xfail-strict em `conftest.py` + tech-debt tracker; pin `pytest-django<4.13` (4.13 exige Django≥5.2, projecto tem 5.1); `docs/plans/` + `QUALITY_SCORE.md` + `.ai/context/backend-logs.md`. Skill `context-migration` → **1.1.0**: novo `reference-hardening.md` (H1 CI, H2 lint arch, H3 docs/plans, H4 quality score, H5 logs) + passo 4c (single) / 10c (monorepo); stub Cursor actualizado (8 ficheiros); cache Claude sincronizada e activada (`installed_plugins.json` → 1.1.0). **Pendente do utilizador:** commits (org-context sozinho; depois workspace + 2 subprojectos via /commit-push) — sem commits locais meus.
